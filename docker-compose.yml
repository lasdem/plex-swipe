services:
  plex-swipe:
    build: .
    container_name: plex-swipe
    ports:
      - "5173:80"
    restart: unless-stopped
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
